/*
 * pwix:field/src/common/js/constants.js
 */

Field.C = {

    // where to insert new specifications when extending a Field.Set
    Insert: {
        AFTER:       0,
        BEFORE:      1
    },

    // verbosity levels
    Verbose: {
        NONE: 0,
        CONFIGURE:      0x01 <<  0,
        FUNCTIONS:      0x01 <<  1
    }
};

// non exported variables

I18N = 'pwix:field:i18n:namespace';
